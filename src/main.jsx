import React from 'react'
import ReactDOM from 'react-dom/client'
import TesseraWorkspace from './TesseraWorkspace.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TesseraWorkspace />
  </React.StrictMode>,
)
